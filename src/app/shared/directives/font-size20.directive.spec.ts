import { FontSize20Directive } from './font-size20.directive';

describe('FontSize20Directive', () => {
  it('should create an instance', () => {
    const directive = new FontSize20Directive();
    expect(directive).toBeTruthy();
  });
});
